# 법률 도메인 환각 탐지 방법론 제안 (v2)
### — 원자적 법률 클레임 검증(Atomic Legal-Claim Verification, ALCV)

> 목적: 기존 3단계(일관성·NER·NLI) OR/앙상블 방식이 실패한 원인을 실측으로 진단하고,
> 선행 연구(FActScore·SAFE·VeriScore·MetaQA)를 참고하여 **타 선행 연구보다 높은 성능으로
> 법률 도메인 환각을 탐지하는** 새 방법론을 제안한다.

---

## 1. 진단 — 현재 방법은 왜 실패하는가 (실측 근거)

800케이스(환각 400/정상 400) 실행 결과, 세 신호의 판별력은 다음과 같다. (AUC 0.5 = 무작위)

| 신호 | AUC | 환각 평균 | 정상 평균 | 진단 |
|------|-----|-----------|-----------|------|
| 일관성(BERTScore) | **0.654** | 0.104 | 0.094 | 약하지만 유일하게 유의미. 단 임계값 0.05에서 환각·정상 **둘 다 100% 발화** → 그대로는 무용 |
| NER 개체 검증 | 0.560 | 0.163 | 0.089 | 거의 무작위. 표적 오류조차 못 잡고 정상에 오발화 |
| NLI 모순 | 0.515 | 0.071 | 0.057 | **사실상 노이즈** |

환각 유형별로 각 신호가 실제로 잡아낸 비율:

| 환각 유형 | 일관성 | NER | NLI |
|-----------|:---:|:---:|:---:|
| 조문번호 오류 (제999조 등) | 1.00 | **0.29** | 0.05 |
| 금지 법령 주입 (무관한 형법) | 1.00 | 0.21 | 0.10 |
| 연락처·기관 불일치 | 1.00 | 0.14 | 0.05 |
| 의미 왜곡 (3년→30년) | 0.99 | 0.13 | 0.14 |

결합 전략 비교에서도 천장이 분명했다: 최선이 AND(F1 0.675)·로지스틱 스태킹(CV F1 0.673)에 머물렀고,
**MetaQA(F1 0.777, P 0.760)에 크게 뒤졌다.** 로지스틱 회귀 가중치 `[일관성 0.49, NER 0.23, NLI 0.09]`,
AND 임계값 `[0.07, −∞, −∞]`는 NER·NLI가 결정에 거의 기여하지 못함을 그대로 보여준다.

### 근본 원인 (세 가지)

1. **NER 검증이 "불완전한 question-level RAG에 대한 exact/fuzzy match"라 구조적으로 깨진다.**
   앞선 RAGAS 평가에서 Context Precision/Recall이 0.33~0.42에 불과했다. 즉 비교 기준이 되는 근거 문서
   자체가 불완전하다. 정답 조문이 검색되지 않으면 → 정상 답변도 "불일치"로 오탐(정상 11% 오발화),
   가짜 조문의 정답 짝이 검색되지 않으면 → 환각도 못 잡는다(조문번호 오류 29%만 탐지). 검증이 검색에
   종속되어 양방향으로 무너진다.

2. **NLI가 "모순(contradiction)"을 보는데, 법률 환각은 대부분 "모순"이 아니라 "근거 부재(미지지)"다.**
   "근로기준법 제999조"는 근거 문서와 *모순*되지 않는다 — 단지 *지지되지 않을* 뿐이다. contradiction
   확률은 거의 오르지 않아(AUC 0.515) 신호가 사라진다. 봐야 할 것은 contradiction이 아니라
   **entailment(지지) 여부**다.

3. **일관성 신호는 임계값이 잘못 설정됐고(0.05 << 실제 분포), 샘플 재생성 방식이 "이 답변의 특정 사실이
   틀렸는지"를 직접 검증하지 못한다.** 질문을 다시 답하게 해 비교하는 것은 사실 단위 검증이 아니다.

> 결론: **문제는 "결합"이 아니라 "신호 자체"다.** NER·NLI를 사실 단위(claim-level) 검증으로
> 교체하지 않는 한, 어떤 앙상블도 F1 ~0.67 천장을 넘지 못한다.

---

## 2. 선행 연구 지형

| 계열 | 대표 연구 | 원리 | 법률 도메인 한계 |
|------|-----------|------|------------------|
| 일관성 기반 | SelfCheckGPT (Manakul 2023) | 샘플 답변 간 일관성 | 모든 샘플이 공유하는 사실 오류 못 잡음 |
| 사실 추출·검증 | **MetaQA** (Yang 2025) | 답변에서 사실 추출 후 외부지식 대조 | 본 실험 최강(F1 0.777). 단 법률 구조(조문 번호) 특화는 없음 |
| **원자적 클레임 검증** | **FActScore** (Min 2023), **SAFE** (Wei 2024), **VeriScore** (Song 2024) | ①원자적 클레임 분해 → ②클레임별 근거 검색 → ③클레임별 검증 | 일반 도메인(위키/웹) 대상. 법령 DB·조문 구조 미활용 |
| 사실 검증 | FEVER (Thorne 2018) | 주장→SUPPORTED/REFUTED/NEI | 도메인 비특화 |
| 법률 특화 | Dahl 2024(*Large Legal Fictions*), HalluDetect 2025 | 인용/조문 환각의 심각성, span 단위 검증의 필요성 | 탐지 파이프라인보다 문제 제기·벤치마크 중심 |

핵심 통찰: 현재 장문 사실성 평가의 **SOTA 패러다임은 "원자적 클레임 분해 + 클레임별 근거 검색 +
클레임별 검증"**이다(FActScore·SAFE·VeriScore). MetaQA가 우리를 이긴 이유도 같은 계열(클레임 단위 검증)
이기 때문이다. **우리가 MetaQA까지 넘으려면, 이 패러다임을 채택하되 법률 도메인 고유의 구조적 검증
(법령 DB·조문 정합성)을 결합해 일반 방법이 갖지 못한 정밀도를 더해야 한다.**

---

## 3. 제안 방법론 — ALCV (Atomic Legal-Claim Verification)

### 핵심 아이디어
답변을 **타입이 부여된 원자적 법률 클레임**으로 분해하고, 각 클레임을 **그 종류에 맞는 방식으로** 검증한다.
구조적 사실(법령명+조문번호, 형량, 금액, 기관)은 **법령 DB 기호 검증**으로, 절차·해석 클레임은
**entailment(지지) 기반 grounding**으로 검증한 뒤, 클레임 결과를 **보정 집계**해 답변 단위로 판정한다.

```
답변
 └─(1) 원자적 법률 클레임 분해 (LLM, 타입 부여)
        c1: [LAW]   "임금체불은 근로기준법 제43조의 적용을 받는다"
        c2: [PENALTY]"위반 시 3년 이하 징역 또는 3천만원 이하 벌금"
        c3: [ORG]   "고용노동부에 진정을 제기할 수 있다"
        c4: [PROC]  "진정은 사업장 관할 지방고용노동청에 한다"
 └─(2) 클레임별 표적 검색 (클레임 자체를 질의로) + 구조적 법령 DB 조회
 └─(3) 타입별 하이브리드 검증
        구조적(LAW/PENALTY/AMOUNT/ORG/DATE) → 기호 검증(존재·정합성)
        해석/절차(PROC/SEMANTIC)            → entailment 기반 grounding
 └─(4) 클레임 결과 → 답변 점수 (보정 집계, 위험가중)
 └─(5) 재현율 하한 제약하 임계값 선택 (안전성)
```

### 3.1 원자적 법률 클레임 분해 (Atomic Legal-Claim Decomposition)
- LLM으로 답변을 **검증 가능한 최소 명제** 단위로 분해하고, 각 클레임에 타입을 부여한다:
  `LAW`(법령+조문), `PENALTY`(형량), `AMOUNT`(금액), `ORG`(기관), `DATE`(기한),
  `PROCEDURE`(절차), `INTERPRETATION`(해석/적용). VeriScore처럼 **검증 가능한 클레임만** 추출한다.
- 기존 question-level 처리가 아니라 **클레임 단위**가 되는 것이 핵심: 잘게 쪼개야 "어느 사실이 틀렸는지"를
  정확히 짚는다(근본원인 1·3 해결).

### 3.2 클레임별 표적 검색 + 구조적 법령 DB 조회 (Claim-Targeted Retrieval)
- **검색 질의를 질문이 아니라 클레임으로** 한다. 질문 단위 RAG의 낮은 정밀도(0.33~0.42)에 검증이
  종속되던 문제를 끊는다(근본원인 1 해결).
- 추가로, 구조적 클레임은 **법령 DB를 직접 조회**한다. 보유한 법령 5,056개 청크를
  `(법령명, 조문번호) → 조문 텍스트` 인덱스로 구조화해두면:
  - `근로기준법 제999조` → **조회 실패 = 존재하지 않는 조문**(조문번호 오류를 **결정적·고정밀**로 탐지)
  - `임금체불`에 `형법 제298조` → **사안-법령 도메인 불일치**(금지법령 주입 탐지)

### 3.3 타입별 하이브리드 검증 (Typed Hybrid Verification)
- **구조적 클레임(LAW/PENALTY/AMOUNT/ORG/DATE)** → **기호(symbolic) 검증**
  - 조문 존재 여부, 사안-법령 정합성, 형량·금액 수치/방향(이상·이하) 정합성을 규칙으로 판정.
  - 이 부분이 일반 방법(SelfCheck·MetaQA)에 없는 **법률 특화 고정밀 레이어**다.
- **해석/절차 클레임(PROCEDURE/INTERPRETATION)** → **entailment 기반 grounding**
  - NLI를 *contradiction*이 아니라 **entailment(지지)** 로 사용한다. 클레임이 근거에 의해
    **지지되지 않으면(entailment < τ) 환각**으로 본다. "모순은 없지만 근거도 없다"를 잡는다(근본원인 2 해결).
  - 즉 각 클레임을 FEVER식 {SUPPORTED / REFUTED / NEI}로 라벨링하되, 법률에선 **NEI도 위험**으로 취급.

### 3.4 클레임 → 답변 보정 집계 (Calibrated Claim Aggregation)
- 클레임별 결과로부터 답변 단위 특징을 만든다:
  `구조적 검증 실패 수`, `미지지(비-entailed) 클레임 비율`, `최소 entailment 점수`,
  `위험가중 실패 점수`(LAW/PENALTY 실패에 높은 가중), 그리고 보조로 `일관성 점수`.
- 작은 **보정 분류기**(로지스틱 회귀)로 결합하되, **고위험 구조적 클레임이 하나라도 실패하면 즉시 환각**
  으로 보는 **하드 게이트**를 둔다(법률 안전성). 일반 클레임은 미지지 비율 임계로 판정.

### 3.5 안전성 제약 (Recall-Constrained Threshold)
- 최종 임계값은 **재현율 하한(τ_R) 제약하 F1 최대화**로 선택(기존 optimize_ensemble 골격 재사용).
- 단, 이번엔 입력 신호가 판별력을 가지므로 같은 재현율에서 **정밀도가 실질적으로 상승**한다.

---

## 4. 왜 기존 실패를 해결하고 SOTA를 넘는가 (매핑)

| 기존 실패 | 원인 | ALCV의 해결 |
|-----------|------|-------------|
| 조문번호 오류 29%만 탐지 | RAG 종속 exact-match | **법령 DB 직접 조회**: 존재하지 않는 조문 = 결정적 탐지 |
| NLI 무작위(AUC 0.52) | contradiction을 봄 | **entailment(미지지)** 로 전환, NEI=위험 |
| 정상 11% 오탐 | question-level RAG 불완전 | **클레임-표적 검색**으로 근거 정밀도 향상 |
| 일관성만 유효 | 사실 단위 검증 부재 | **원자적 클레임 단위** 검증 |
| MetaQA에 패배 | 클레임 검증 부재 | 클레임 검증 채택(=MetaQA 동급) **+ 법률 구조 기호검증(=MetaQA 초과 여지)** |

ALCV는 SOTA 패러다임(원자적 클레임 검증)을 법률 도메인에 이식하면서, **일반 방법이 가질 수 없는
법령 DB 기호 검증**을 더한다. 조문번호·금지법령 오류처럼 법률에서 가장 치명적이고 가장 빈번한
환각 유형이 바로 이 기호 검증으로 고정밀 탐지되므로, MetaQA를 포함한 일반 방법 대비 **법률 도메인에서**
우위를 기대할 수 있다.

---

## 5. 구현 · 평가 계획 (기존 코드에 매핑)

검증 가능한 목표로 분해(프로젝트 규칙):

1. **법령 DB 인덱스 구축** → 검증: `(법령명, 조문번호)` 조회가 존재 조문은 hit, `제999조`는 miss.
   - 입력: `data/real_data/New_Dataset/rag_law_chunks*.jsonl` 등. 산출: `law_index.json`.
2. **클레임 분해기** `claim_decomposer.py` → 검증: 표본 답변에서 타입부여 클레임 3~8개 추출, 사람 점검.
3. **타입별 검증기** `claim_verifier.py`
   - 기호 검증: 조문 존재·도메인 정합·수치 정합(기존 `ner_checker`의 규칙 레이어 재활용).
   - entailment 검증: 기존 NLI 모델을 **entailment 헤드**로 사용(contradiction→entailment 전환).
   - 검증: 조문번호 오류/금지법령 유형에서 탐지율 ≥0.9 목표.
4. **신호 수집 v2** `evaluate_alcv_signals.py` → 클레임 특징 저장(기존 signals 스크립트 확장).
5. **집계·평가** `optimize_ensemble.py` 재사용(입력 특징만 교체) → 검증: **F1 > MetaQA(0.777)**,
   재현율 ≥ τ_R. 유형별 탐지율·정밀도 표 동반.
6. **논문 반영**: 방법론을 v6 docx의 §3로 교체, 결과를 §4 표/그림에 채움.

> 우선 1·2·3을 작은 표본(예: 50케이스)으로 검증해 entailment·기호검증의 유형별 탐지율을 확인한 뒤,
> 전체 800케이스로 확장하는 것을 권한다(비용·리스크 관리).

---

## 6. 참고문헌 (핵심)

- Min et al., "FActScore: Fine-grained Atomic Evaluation of Factual Precision in Long Form Text Generation," EMNLP 2023.
- Wei et al., "Long-form factuality in large language models (SAFE)," NeurIPS 2024.
- Song et al., "VeriScore: Evaluating the factuality of verifiable claims in long-form text generation," Findings of EMNLP 2024.
- Manakul et al., "SelfCheckGPT," EMNLP 2023.
- Yang et al., "Hallucination Detection in LLMs with Metamorphic Relations (MetaQA)," ACM FSE 2025.
- Thorne et al., "FEVER: a Large-scale Dataset for Fact Extraction and VERification," NAACL-HLT 2018.
- Dahl et al., "Large Legal Fictions: Profiling Legal Hallucinations in LLMs," Journal of Legal Analysis, 2024.
- "HalluDetect: Detecting, Mitigating, and Benchmarking Hallucinations in Conversational Systems in the Legal Domain," 2025.
