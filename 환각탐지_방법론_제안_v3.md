# 법률 도메인 환각 탐지 방법론 v3
### — 실제 환각 기반 평가셋 + 3단 하이브리드 검증 (ALCV-v2)

> v2(ALCV)의 치명적 결함인 **데이터셋–방법론 순환성**을 제거하기 위해, 평가 데이터셋을
> 규칙 주입 방식에서 **실제 LLM 환각 수집** 방식으로 전면 교체하고, 검증 레이어를
> **3단 하이브리드(DB 결정적 → 로컬 NLI → LLM 근거-대조)**로 재설계한다.
> 브랜치: `methodology-v3` (2026-07-06 합의)

---

## 1. v2의 문제 (전제)

1. **순환성**: 평가셋 800건의 환각이 규칙 주입 4유형(조문번호·금지법령·의미왜곡·연락처, 각 100건)인데,
   ALCV의 결정적 규칙이 그 주입 규칙의 역설계였다. 탐지기가 생성기를 맞추는 구조.
2. **실제 환각과의 괴리**: 실제 법률 LLM 환각은 그럴듯한 해석 오류·법리 혼동·구법 인용 등
   미묘한 형태가 다수(Dahl 2024). 기존 셋에 전무.
3. **50:50 인위적 균형**: 실제 환각률과 다른 분포로 Precision 추정 왜곡.

**재사용 자산**: RAG 코퍼스(법령 5,056 / 판례 28,711 / 매뉴얼 8,132 청크), 법령 DB 인덱스,
MetaQA·SelfCheck 베이스라인, ALCV 클레임 분해기.

---

## 2. 합의된 설계 결정 (2026-07-06)

| 결정 사항 | 선택 | 근거 |
|---|---|---|
| 평가셋 구축 | 실제 LLM 환각 수집(주력) + 외부 공개 벤치마크(보조) | 순환성 제거, 일반화 증명 |
| 답변 생성 모델 | GPT-4o-mini 단독 | 비용·환경 제약. "단일 모델 환각" 한계는 논문에 명시 |
| verdict 레이어 | 3단 하이브리드 (DB → 로컬 NLI → LLM 근거-대조) | 비용 최소화 + LLM은 좁은 entailment 과제만 수행 |
| 수동 라벨링 | 최소화 (스팟체크 150~200건만) | 시간 제약. silver 라벨 품질을 스팟체크로 정량 보고 |
| 기존 800건 | 폐기하지 않고 "통제된 규칙 주입 실험"으로 강등 | sanity check 및 v2 대비 실험 |

### LLM 사용에 대한 경쟁력 논거 (핵심 질문에 대한 답)

베이스라인은 **같은 백본(GPT-4o-mini)의 vanilla judge**이므로 비교가 공정하며, 우리가 이기는
구조적 이유는 백본이 아니라 **접지(grounding)**에 있다:

1. 조문 존재·번호·시행일·수치는 **법령 DB 조회가 LLM보다 항상 정확** — vanilla judge가 구조적으로 못 이기는 영역.
2. LLM은 자유 판정이 아니라 **"이 클레임이 이 검색 근거에 지지되는가"**라는 좁은 과제만 수행 —
   근거 없이 판단하는 vanilla judge 대비 우위는 FActScore/SAFE 계열의 일관된 결과.
3. 논문 스토리: **"약한 백본 + 강한 도메인 접지 > 백본 단독"**. GPT-4o-mini는 한국 법령
   파라메트릭 지식이 약하므로 오히려 이 대비가 선명해진다.

리스크: 미묘한 해석 클레임은 백본 능력이 상한. Stage 2(로컬 NLI)가 1차 방어하고 LLM은
불확실 구간 tie-breaker로만 사용해 노출을 줄인다.

---

## 3. 새 평가 데이터셋

### 3.1 Track A — 실제 환각 수집 (주 평가셋)

```
코퍼스 청크(gold) ─(1) 질문 생성─→ 질문 300~500
                                     │
        (2) 답변 생성: GPT-4o-mini × {closed-book, 약한 RAG(top-1)} × 온도 다양화
                                     │
        (3) 클레임 분해 (기존 ALCV 분해기, 타입 태깅)
                                     │
        (4) 라벨링 3층
             L1 결정적: 법령DB 대조로 확정 가능한 클레임 → gold 라벨 (자동)
             L2 근거기반 silver: 해석·절차 클레임 → gold 소스청크+조문 전문 대조
                entailment, self-consistency 3표결 (자동)
             L3 수동 스팟체크: L2 표본 150~200건 검수 → 라벨 정확도(agreement) 보고
```

**순환성 회피 원칙 — 생성·라벨·탐지의 3자 분리:**

- **생성**: 규칙 주입 없음. 자연 발생 환각만.
- **라벨러**: gold 소스 청크를 **제공받은 상태**에서 판정 (검색 불확실성 없음).
- **탐지기**: gold를 모르고 **검색부터** 수행. → 라벨러와 탐지기 사이에 정보 비대칭이 있어
  "탐지기가 라벨러를 복제"하는 구조가 아님.

**남는 약점(명시)**: L2 라벨러와 Stage 3 판정이 같은 백본(4o-mini). 완화책 — (a) L2는
self-consistency 3표결 + gold 근거 제공으로 과제 난이도 자체가 다름, (b) L3 스팟체크로
라벨 오류율을 정량 보고, (c) API 잔량 확인 후 가능하면 라벨러만 상위 모델로 교체(선택).

**분포**: 50:50 강제하지 않고 자연 발생 비율 유지. 클레임 수 목표 2,000~3,000개
(질문 300~500 × 답변 2조건 × 클레임 3~5개).

### 3.2 Track B — 외부 공개 벤치마크 (일반화 증명)

| 벤치마크 | 성격 | 용도 |
|---|---|---|
| **TriBench-Ko** (2026, github.com/holi-lab/TriBench-Ko, 공개) | 한국 사법 워크플로 4과제 × 리스크 8유형(환각 포함), 실제 판결문 기반 | **주 외부 평가.** 제3자 구축이라 우리 순환성 없음 |
| **K-HALU** (2,170샘플, 한국어 일반 도메인) | 문서-진술 환각 판정 | 도메인 외 일반화 테스트(보조) |
| K-FinHallu (한국어 금융 RAG 환각) | 멀티턴 RAG 환각 탐지 | 도메인 전이 테스트(선택) |

각 벤치마크는 데이터 접근성·라이선스·과제 형식 호환성을 착수 시 재확인한다.

### 3.3 기존 800건

부록의 통제 실험으로 강등: "주입 유형별 탐지율" 분석 및 v2 방법과의 대조용으로만 사용.

---

## 4. 검증 파이프라인 — 3단 하이브리드

```
클레임 (타입 태깅됨)
 ├─ Stage 1 결정적 (규칙/DB): 법령명+조문 존재, 조문번호 정합, 시행일, 수치·형량
 │    → 법령 DB 인덱스 조회로 SUPPORTED / REFUTED 확정. LLM 미사용, 비용 0
 ├─ Stage 2 로컬 NLI: Stage 1로 확정 안 되는 클레임 → 검색 근거와 entailment 판정
 │    (v2 교훈 반영: contradiction이 아니라 entailment 부재 = NEI를 본다)
 │    확신도 높으면 확정, 낮으면 ↓
 └─ Stage 3 LLM 근거-대조 (GPT-4o-mini): [클레임 + 검색 근거] 제시
      → SUPPORTED / NOT_SUPPORTED / NEI. 자유 판정 금지, 근거 내 판단만
집계: REFUTED > NEI 가중으로 답변 단위 환각 점수 산출, 확률 보정(calibration)
```

## 5. 베이스라인 및 비교 설계

| 베이스라인 | 목적 |
|---|---|
| SelfCheckGPT | 일관성 계열 대표 (기존 코드 재사용) |
| MetaQA | v2에서 최강이었던 비교 대상 (F1 0.777) |
| **GPT-4o-mini vanilla judge** (zero-shot, 근거 없음) | "LLM 하이브리드가 vanilla judge를 이기는가" 핵심 대조 |
| GPT-4o-mini + naive RAG judge | 접지의 구조(파이프라인)가 단순 RAG 판정보다 나은지 분리 검증 |

**지표**: PR-AUC(주), 운영점 F1/Precision/Recall, 클레임 단위 + 답변 단위 병기,
환각 유형별(구조적 vs 해석적) 분해 성능.

**성공 기준**: Track A에서 MetaQA와 vanilla judge를 PR-AUC 기준 상회.
특히 "해석적 환각" 하위집합에서의 우위가 핵심 기여.

## 6. 비용 추정 (GPT-4o-mini: $0.15/1M in, $0.60/1M out)

답변 생성(500질문 × 2조건 × ~600tok) + 클레임 분해 + L2 라벨 3표결 + Stage 3 판정 +
베이스라인 judge 실행 전부 합산해도 **수 달러 이내**. 토큰 잔량이 병목일 가능성 낮음.
착수 전 API 사용량 대시보드에서 잔여 크레딧 확인.

## 7. 실행 계획 (단계 → 검증 방법)

1. **질문 생성 파이프라인** (`scripts_hallucination/collect/`) → 검증: 질문 50개 표본 수동 확인(코퍼스로 답 가능한가)
2. **답변 생성 + 클레임 분해** → 검증: 분해 클레임 30개 표본이 원문과 의미 일치하는지 확인
3. **L1/L2 자동 라벨링** → 검증: L3 스팟체크 150~200건, agreement ≥ 0.85 목표. 미달 시 L2 프롬프트 개선 후 재라벨
4. **Track B 데이터 확보** (TriBench-Ko, K-HALU) → 검증: 로드·형식 변환 후 샘플 10건 육안 확인
5. **3단 하이브리드 구현** (Stage 1·2는 기존 코드 개조) → 검증: 기존 800건 sanity check에서 v2 결정적 규칙 성능 이상 유지
6. **베이스라인 4종 실행 + 본 평가** → 검증: 성공 기준(§5) 달성 여부, 유형별 분해표
7. **결과 분석 → 기여 방향 확정** (사용자 의사: 결과 보고 결정)

## 8. 명시적 한계 (논문에 기재)

- 환각 생성이 GPT-4o-mini 단독 → "단일 모델 환각 분포" 한계. Track B 외부 벤치마크로 보완.
- L2 silver 라벨 → 스팟체크 기반 라벨 오류율을 함께 보고.
- 라벨러·판정기 백본 동일 → §3.1 완화책 및 정보 비대칭 구조로 방어.
