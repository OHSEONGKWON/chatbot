# 평가 방법론 상세 설명

## 2. RAGAS End-to-End 평가

### 📖 개요
RAGAS는 RAG(Retrieval-Augmented Generation) 시스템을 종합적으로 평가하는 프레임워크입니다. 검색 품질과 생성 품질을 모두 측정합니다.

---

### 📊 평가 메트릭 설명

#### 2.1 Faithfulness (충실도)
**정의:** 생성된 답변이 검색된 문서(Context)에 얼마나 충실한가?

**측정 방법:**
```
1. 답변을 개별 진술(statement)로 분해
2. 각 진술이 검색된 문서에서 뒷받침되는지 확인
3. Faithfulness = 뒷받침되는 진술 수 / 전체 진술 수
```

**결과 해석:**
```
BM25-RAG:   0.9596  (95.96% 충실)
Dense-RAG:  0.9717  (97.17% 충실) ← 가장 높음
Hybrid-RAG: 0.9505  (95.05% 충실)
```

**의미:**
- ✅ 모든 방법이 0.95 이상 → 환각 거의 없음
- ✅ Dense-RAG가 가장 높음 → 의미 기반 검색이 정확한 문서 검색
- 💡 **법률 도메인에서 매우 중요** (잘못된 정보는 치명적)

---

#### 2.2 Answer Relevancy (답변 관련성)
**정의:** 생성된 답변이 질문과 얼마나 관련이 있는가?

**측정 방법:**
```
1. 답변으로부터 역으로 질문을 생성
2. 생성된 질문과 원래 질문의 유사도 측정
3. Answer Relevancy = 평균 유사도
```

**결과 해석:**
```
BM25-RAG:   0.9182  (91.82% 관련)
Dense-RAG:  0.9455  (94.55% 관련) ← 가장 높음
Hybrid-RAG: 0.9222  (92.22% 관련)
```

**의미:**
- ✅ 모든 방법이 0.91 이상 → 질문에 잘 맞는 답변
- ✅ Dense-RAG가 가장 높음 → 의미 기반 검색이 관련성 높은 답변 생성
- 💡 사용자 만족도와 직결

---

#### 2.3 Context Precision (컨텍스트 정밀도)
**정의:** 검색된 문서 중 실제로 유용한 문서의 비율

**측정 방법:**
```
1. 검색된 Top-K 문서 중 정답 생성에 사용된 문서 확인
2. Context Precision = 유용한 문서 / 전체 검색 문서
```

**결과 해석:**
```
BM25-RAG:   0.3826  (38.26%)
Dense-RAG:  0.3384  (33.84%)
Hybrid-RAG: 0.4162  (41.62%) ← 가장 높음
```

**의미:**
- ⚠️ 모든 방법이 0.3~0.4 → 검색된 문서 중 60% 이상 불필요
- ✅ Hybrid가 가장 높음 → BM25 + Dense 조합이 효과적
- 💡 개선 여지 있음 (재순위화, 필터링 등)

---

#### 2.4 Context Recall (컨텍스트 재현율)
**정의:** 정답 생성에 필요한 모든 정보를 검색했는가?

**측정 방법:**
```
1. 정답에 필요한 모든 정보(ground truth) 확인
2. 검색된 문서가 그 정보를 포함하는지 확인
3. Context Recall = 검색된 정보 / 필요한 정보
```

**결과 해석:**
```
BM25-RAG:   0.3826  (38.26%)
Dense-RAG:  0.3384  (33.84%)
Hybrid-RAG: 0.4162  (41.62%) ← 가장 높음
```

**의미:**
- ⚠️ 필요한 정보의 40%만 검색 → 개선 필요
- 💡 Top-K 증가 또는 재검색 전략 고려

---

#### 2.5 Citation Accuracy (인용 정확도)
**정의:** 답변에 포함된 인용(출처)이 정확한가?

**측정 방법:**
```
1. 답변에서 인용된 법령/조문 추출
2. 검색된 문서에 해당 법령/조문이 존재하는지 확인
3. Citation Accuracy = 정확한 인용 / 전체 인용
```

**결과 해석:**
```
BM25-RAG:   0.5152  (51.52%) ← 가장 높음
Dense-RAG:  0.1818  (18.18%)
Hybrid-RAG: 0.3283  (32.83%)
```

**의미:**
- ⚠️ Dense-RAG가 매우 낮음 → 의미 검색이 정확한 조문 매칭에 약함
- ✅ BM25가 가장 높음 → 키워드 매칭이 법령 인용에 유리
- 💡 **법률 도메인에서 매우 중요** (조문 번호 정확성)

---

### 🎯 RAGAS 종합 평가

**최고 성능:**
- **Faithfulness/Relevancy:** Dense-RAG (의미 기반이 답변 품질 우수)
- **Context Precision/Recall:** Hybrid-RAG (검색 정확도 우수)
- **Citation Accuracy:** BM25-RAG (법령 인용 정확)

**권장 전략:**
```
1차 검색: Hybrid (폭넓게)
2차 재순위: BM25 (법령 조문 우선)
생성: Dense 기반 (의미 정확성)
```

---

---

## 3. 환각 탐지 성능 평가

### 📖 개요
LLM이 생성한 답변에서 사실과 다른 정보(환각, Hallucination)를 탐지하는 능력을 평가합니다.

---

### 📊 평가 데이터셋

**규모:** 800개 케이스
- 환각 케이스: 400개
- 정상 케이스: 400개

**환각 유형:**
```
1. article_number_error (100개)
   예: "근로기준법 제999조" (존재하지 않는 조문)

2. contact_mismatch (100개)
   예: 잘못된 연락처, 기관명

3. forbidden_law_injection (100개)
   예: "형법 제298조" (임금체불 질문에 성범죄 법 언급)

4. semantic_error (100개)
   예: "3년 이하 징역" → "30년 이하 징역" (의미 왜곡)
```

---

### 🔬 평가 방법론

#### 3.1 우리 모델 (BERTScore + NER + NLI)

**구성:**
```
1단계: BERTScore 일관성 체크
  └─ 동일 질문에 5개 샘플 답변 생성
  └─ 원래 답변과 샘플들의 의미 유사도 측정
  └─ 유사도 낮으면 → 환각 의심

2단계: NER (법률 엔티티 검증)
  └─ 답변에서 법령/조문/형량 등 추출
  └─ RAG 문서에 해당 엔티티 존재 확인
  └─ 없으면 → 환각

3단계: NLI (의미 모순 검사)
  └─ 답변과 RAG 문서의 의미 비교
  └─ 모순되면 → 환각

최종 판정: 1 OR 2 OR 3 (하나라도 환각이면 환각)
```

**Threshold:**
- BERTScore: 0.05 (매우 낮게 설정 → Recall 극대화)

**결과:**
```
Precision: 0.502  (환각이라 판정한 것 중 50.2%가 진짜 환각)
Recall:    0.998  (실제 환각 400개 중 399개 탐지)
F1-Score:  0.668

TP: 399 (환각을 환각으로 정확히 판정)
FP: 396 (정상을 환각으로 오판)
FN: 1   (환각을 정상으로 오판) ← 거의 없음!
TN: 4   (정상을 정상으로 정확히 판정)
```

**특징:**
- ✅ **Recall 0.998 = 거의 완벽한 환각 탐지**
  - 400개 환각 중 399개 탐지
  - 환각 놓침(FN) 단 1개!
- ⚠️ **Precision 0.502 = 높은 오탐율**
  - 정상 케이스 400개 중 396개를 환각으로 오판
  - 정상 중 99%를 환각으로 판정
- 💡 **법률 도메인 전략: 안전 우선**
  - 환각을 놓치는 것(위험) > 정상을 환각으로 오판(안전)
  - 보수적 판정으로 사용자 보호

---

#### 3.2 SelfCheckGPT-NLI

**구성:**
```
1. 동일 질문에 5개 샘플 답변 생성
2. 원래 답변을 문장으로 분해
3. 각 문장 × 각 샘플 → NLI 모델로 모순 점수 계산
4. 모순 점수 > Threshold → 환각
```

**논문 기반:**
- Section 5.4: SelfCheckGPT with NLI
- 원리: LLM이 사실을 알면 샘플들이 일관적, 환각이면 샘플들이 모순

**Threshold:**
- 0.15 (낮게 설정 → Recall 극대화)

**결과:**
```
Precision: 0.517  (51.7%)
Recall:    0.910  (91.0%)
F1-Score:  0.659

TP: 364 (환각 탐지)
FP: 340 (오탐)
FN: 36  (환각 놓침)
TN: 60  (정상 정확 판정)
```

**특징:**
- ✅ Recall 0.910 = 환각의 91% 탐지
- ⚠️ Precision 0.517 = 절반 정도 오탐
- 💡 범용 방법론 (도메인 무관)

---

#### 3.3 MetaQA

**구성:**
```
1. 답변에서 사실(fact) 추출
2. 각 사실을 외부 지식베이스로 검증
3. 검증 실패하면 → 환각
```

**결과:**
```
Precision: 0.775  (77.5%)
Recall:    0.793  (79.3%)
F1-Score:  0.784  ← 가장 높음!

균형잡힌 성능
```

**특징:**
- ✅ 가장 높은 F1-Score
- ✅ Precision/Recall 균형
- 💡 사실 기반 검증 (가장 신뢰)

---

### 🎯 환각 탐지 종합 평가

**순위:**
```
1위: MetaQA           (F1 0.784) - 가장 균형잡힘
2위: 우리 모델         (F1 0.668) - 환각 거의 완벽 탐지
3위: SelfCheckGPT-NLI (F1 0.659) - 범용성 우수
```

**비교 분석:**

| 모델 | 강점 | 약점 | 적합한 사용 사례 |
|------|------|------|------------------|
| **MetaQA** | • 균형잡힌 성능<br>• 높은 신뢰도 | • 외부 지식베이스 필요 | • 프로덕션 환경<br>• 정확성 중요 |
| **우리 모델** | • **환각 거의 놓치지 않음**<br>• 법률 특화(NER) | • 오탐 많음<br>• 속도 느림 | • **법률/의료 도메인**<br>• 안전성 최우선 |
| **SelfCheckGPT-NLI** | • 범용성<br>• 외부 자원 불필요 | • 중간 성능 | • 범용 도메인<br>• 빠른 구현 |

---

### 💡 실전 활용 전략

**시나리오 1: 법률 상담 (우리 모델 사용)**
```
사용자 질문: "임금을 받지 못했는데 어떻게 해야 하나요?"
LLM 답변 생성 → 환각 탐지 → 환각 의심 시 경고 표시

이점:
- Recall 0.998로 잘못된 법률 정보 거의 차단
- 사용자 보호 (잘못된 법률 조언 방지)

단점:
- 정상 답변도 환각으로 오판 가능
- 해결: "확실하지 않음" 표시 후 변호사 상담 권유
```

**시나리오 2: 일반 정보 제공 (MetaQA 사용)**
```
균형잡힌 성능으로 사용자 경험 최적화
```

**시나리오 3: 하이브리드 전략**
```
1차: 우리 모델 (Recall 0.998)
  └─ 환각 의심 케이스 필터링

2차: 의심 케이스에 대해 MetaQA 재검증
  └─ Precision 향상

최종: 정확도 + 안전성 모두 확보
```

---

## 📈 개선 방향

### RAGAS (2번 평가)
```
Context Precision/Recall 개선:
- Top-K 증가 (5 → 10)
- 재순위화 알고리즘 도입
- 쿼리 확장 (동의어, 관련 조문)

Citation Accuracy 개선:
- 법령 조문 매칭 강화
- BM25 가중치 증가
```

### 환각 탐지 (3번 평가)
```
우리 모델 Precision 개선:
- BERTScore Threshold 조정 (0.05 → 0.08)
- NER 정밀도 향상
- 앙상블: 2개 이상 모듈이 환각 판정 시에만 환각

SelfCheckGPT Recall 개선:
- Threshold 추가 하향 (0.15 → 0.12)
- 샘플 수 증가 (5 → 10)
```

---

## 🎓 결론

**RAGAS 평가:**
- Faithfulness/Relevancy 0.9+ → 답변 품질 우수
- Context Precision/Recall → 검색 개선 여지 있음
- 법률 도메인에서 BM25 중요성 확인 (Citation)

**환각 탐지:**
- 우리 모델: **Recall 0.998로 법률 도메인에 최적**
- MetaQA: 균형잡힌 성능으로 프로덕션 적합
- SelfCheckGPT: 범용성으로 빠른 구현 가능

**핵심 메시지:**
> 법률 도메인에서는 **안전성(Recall)이 정확성(Precision)보다 중요**합니다.
> 우리 모델의 Recall 0.998은 사용자를 잘못된 법률 정보로부터 보호합니다.
